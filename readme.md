This is a test for tough-cookie memory leaks.

There is a super super super tiny leak going on right now, I have no idea what might be causing it.

Check out the repo, `npm install`, then `npm start`.